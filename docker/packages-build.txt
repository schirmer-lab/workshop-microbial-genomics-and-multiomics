# Build-time system packages (only needed during image construction)
# These packages are NOT included in the final runtime image

# Essential build tools (needed for compiling R packages and other tools)
build-essential
git

# Locales for build-time operations
locales

# R development packages (needed for R package compilation)
r-base-dev

# System libraries for R packages (build dependencies)
libcurl4-openssl-dev
libssl-dev
libxml2-dev
libfontconfig1-dev
libharfbuzz-dev
libfribidi-dev
libfreetype6-dev
libpng-dev
libtiff-dev
libjpeg-dev

# Additional system libraries for bioinformatics tools compilation
libgit2-dev
libssh2-1-dev
libsasl2-dev
libmagick++-dev

# System libraries that might be needed for compilation
zlib1g-dev
libbz2-dev
liblzma-dev
libncurses-dev
autoconf
pkg-config
libhdf5-dev